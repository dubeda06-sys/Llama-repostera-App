{
  "name": "llama-repostera",
  "version": "2.0.0",
  "private": true,
  "description": "Llama Repostera — gestor de costos de repostería (PWA en Firebase Hosting)",
  "scripts": {
    "serve": "firebase emulators:start --only hosting",
    "deploy": "firebase deploy --only hosting",
    "test:parser": "node tools/test-parser.mjs"
  }
}
